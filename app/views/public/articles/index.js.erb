jQuery(document).bind('ready ajaxComplete', function() {
    $(".tag_name").html("<%= j(render 'public/articles/article-index', article: @article) %>");
    $('.select-drop').change(function() {$(this).parent().submit();});
});